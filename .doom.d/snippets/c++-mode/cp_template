# -*- mode: snippet -*-
# name: cp_template
# uuid: f28e8b1e-1a9d-3e3d-2b91-1bc3e90a25de
# key: /cpp
# condition: t
# --
#include <bits/stdc++.h>
using namespace std;

int32_t main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int _tcs = 1;
  cin >> _tcs;
  for (int _tc = 0; _tc < _tcs; _tc++) {
    $1
  }
  return 0;
}
